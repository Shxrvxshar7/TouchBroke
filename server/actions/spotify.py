# ── SPOTIFY ACTIONS ──────────────────────────────────────────────
# Placeholder — full implementation in Phase 5

async def handle_spotify_action(action, value=None):
    print(f"[spotify] action={action} value={value}")